import styled from "styled-components";

export const StyledCircle5 = styled.div`
  width: ${props => props.width}px;
  height: ${props => props.height}px;
`;

export const Circle = styled.canvas`
  position: absolute;
  top: 0;
  left: 0;
`;
